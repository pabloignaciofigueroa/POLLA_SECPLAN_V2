<aside class="official-access" data-official-access-panel hidden>
  <div class="official-access__identity">
    <span class="official-access__shield" aria-hidden="true">
      <svg viewBox="0 0 24 24" focusable="false">
        <path d="M12 2 4.5 5v5.2c0 5.1 3.2 9.7 7.5 11.8 4.3-2.1 7.5-6.7 7.5-11.8V5L12 2Zm3.7 6.9-4.4 5.2a1 1 0 0 1-1.5.1l-2.2-2.1 1.4-1.5 1.4 1.4 3.8-4.5 1.5 1.4Z" />
      </svg>
    </span>
    <div>
      <p class="official-access__eyebrow" data-official-access-eyebrow>
        Cartón oficial confirmado
      </p>
      <h2 data-official-access-title>Predicciones protegidas</h2>
      <p data-official-access-copy>
        Puedes recorrer tus 72 marcadores y clasificados. La edición requiere un código temporal
        generado por Admin.
      </p>
    </div>
  </div>

  <div class="official-access__tools">
    <div class="official-access__links">
      <a href="/estadisticas">Ir a Estadísticas</a>
      <a href="/tabla">Ver Tabla Dinámica</a>
    </div>

    <form class="edit-code-form" data-edit-code-form>
      <label for="prediction-edit-code">Tengo un código de edición</label>
      <div>
        <input
          id="prediction-edit-code"
          name="code"
          type="text"
          inputmode="text"
          autocomplete="one-time-code"
          minlength="8"
          maxlength="8"
          pattern="[A-Za-z0-9]{8}"
          placeholder="AB12CD34"
          data-edit-code-input
        />
        <button type="submit" data-edit-code-submit>Desbloquear</button>
      </div>
    </form>

    <p class="edit-code-status" data-edit-code-status role="status" aria-live="polite"></p>
    <p class="edit-session-note" data-edit-session-note hidden>
      Edición autorizada hasta <strong data-edit-session-expiry>--:--</strong>. La corrección solo
      será oficial después de que Admin importe el nuevo JSON.
    </p>
  </div>
</aside>

<style>
  .official-access {
    display: grid;
    grid-template-columns: minmax(0, 1.3fr) minmax(19rem, 0.7fr);
    gap: clamp(0.9rem, 2vw, 1.4rem);
    align-items: center;
    padding: clamp(0.85rem, 1.8vw, 1.15rem);
    border: 2px solid var(--pm-purple-500);
    border-radius: var(--pm-radius-lg);
    background:
      radial-gradient(circle at 10% 0%, rgba(124, 53, 255, 0.15), transparent 34%),
      rgba(255, 255, 255, 0.94);
    box-shadow: 0 18px 42px rgba(45, 25, 110, 0.14);
  }

  .official-access[hidden] {
    display: none;
  }

  .official-access[data-access-state="official-editing"] {
    border-color: var(--pm-green-500);
    background:
      radial-gradient(circle at 10% 0%, rgba(34, 197, 94, 0.16), transparent 34%),
      rgba(255, 255, 255, 0.96);
  }

  .official-access__identity {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 0.8rem;
    align-items: center;
  }

  .official-access__shield {
    display: grid;
    width: 3rem;
    aspect-ratio: 1;
    place-items: center;
    border-radius: 999px;
    background: linear-gradient(135deg, var(--pm-purple-500), var(--pm-blue-600));
    color: white;
    box-shadow: 0 10px 24px rgba(124, 53, 255, 0.24);
  }

  [data-access-state="official-editing"] .official-access__shield {
    background: linear-gradient(135deg, var(--pm-green-500), var(--pm-cyan-500));
    color: var(--pm-blue-950);
  }

  .official-access__shield svg {
    width: 1.55rem;
    fill: currentColor;
  }

  .official-access p,
  .official-access h2 {
    margin: 0;
  }

  .official-access__eyebrow {
    color: var(--pm-purple-600);
    font-size: 0.68rem;
    font-weight: 950;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  [data-access-state="official-editing"] .official-access__eyebrow {
    color: var(--pm-green-600);
  }

  .official-access h2 {
    margin-top: 0.18rem;
    color: var(--pm-blue-950);
    font-family: var(--font-display);
    font-size: clamp(1.35rem, 2.4vw, 2rem);
    font-weight: 950;
    line-height: 0.95;
    text-transform: uppercase;
  }

  [data-official-access-copy] {
    margin-top: 0.4rem;
    color: var(--pm-blue-800);
    font-size: 0.84rem;
    font-weight: 750;
    line-height: 1.4;
  }

  .official-access__tools {
    display: grid;
    gap: 0.55rem;
  }

  .official-access__links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
  }

  .official-access__links a {
    padding: 0.48rem 0.7rem;
    border: 1px solid var(--pm-blue-300);
    border-radius: var(--pm-radius-pill);
    background: white;
    color: var(--pm-blue-700);
    font-size: 0.7rem;
    font-weight: 950;
    text-transform: uppercase;
  }

  .edit-code-form {
    display: grid;
    gap: 0.32rem;
  }

  .edit-code-form[hidden] {
    display: none;
  }

  .edit-code-form label {
    color: var(--pm-blue-900);
    font-size: 0.7rem;
    font-weight: 950;
    text-transform: uppercase;
  }

  .edit-code-form > div {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 0.4rem;
  }

  .edit-code-form input {
    min-width: 0;
    min-height: 2.55rem;
    padding: 0.55rem 0.75rem;
    border: 2px solid var(--pm-purple-400);
    border-radius: var(--pm-radius-sm);
    background: white;
    color: var(--pm-blue-950);
    font-family: var(--font-score);
    font-size: 1rem;
    font-weight: 950;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .edit-code-form button {
    min-height: 2.55rem;
    padding: 0.55rem 0.8rem;
    border: 0;
    border-radius: var(--pm-radius-sm);
    background: linear-gradient(135deg, var(--pm-purple-500), var(--pm-pink-500));
    color: white;
    font-family: var(--font-display);
    font-size: 0.75rem;
    font-weight: 950;
    cursor: pointer;
    text-transform: uppercase;
    transition: transform 140ms cubic-bezier(0.23, 1, 0.32, 1);
  }

  .edit-code-form button:active,
  .official-access__links a:active {
    transform: scale(0.97);
  }

  .edit-code-form input:focus-visible,
  .edit-code-form button:focus-visible,
  .official-access__links a:focus-visible {
    outline: none;
    box-shadow: var(--pm-focus-ring-pink);
  }

  .edit-code-status {
    min-height: 1em;
    color: var(--pm-red-600);
    font-size: 0.72rem;
    font-weight: 850;
  }

  .edit-session-note {
    padding: 0.5rem 0.6rem;
    border-radius: var(--pm-radius-sm);
    background: color-mix(in srgb, var(--pm-green-500) 12%, white);
    color: var(--pm-blue-900);
    font-size: 0.74rem;
    font-weight: 800;
    line-height: 1.35;
  }

  .edit-session-note[hidden] {
    display: none;
  }

  @media (max-width: 840px) {
    .official-access {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 520px) {
    .edit-code-form > div {
      grid-template-columns: 1fr;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .edit-code-form button,
    .official-access__links a {
      transition: none;
    }
  }
</style>
