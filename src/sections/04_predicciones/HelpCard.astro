---
---

<aside class="help-card" aria-label="Ayuda de predicciones">
  <h2>¿NECESITAS AYUDA?</h2>
  <p>Revisa las reglas y consejos para tener la mejor estrategia.</p>
  <a href="/reglas">VER AYUDA →</a>
</aside>

<style>
  .help-card {
    padding: 1rem;
    border: 1px solid var(--pm-cyan-600);
    border-radius: var(--pm-radius-md);
    background: linear-gradient(135deg, var(--pm-blue-500), var(--pm-cyan-500));
    color: var(--pm-white);
    box-shadow: var(--pm-shadow-cyan);
  }

  h2,
  p {
    margin: 0;
  }

  h2 {
    color: var(--pm-white);
    font-family: var(--font-display);
    font-size: 0.95rem;
    font-weight: 950;
    letter-spacing: 0.04em;
  }

  p {
    margin-top: 0.45rem;
    color: rgba(255, 255, 255, 0.92);
    font-size: 0.82rem;
    line-height: 1.4;
  }

  a {
    display: inline-flex;
    margin-top: 0.65rem;
    padding: 0.3rem 0.7rem;
    border: 1px solid rgba(255, 255, 255, 0.55);
    border-radius: var(--pm-radius-pill);
    background: rgba(255, 255, 255, 0.15);
    color: var(--pm-white);
    font-family: var(--font-display);
    font-size: 0.82rem;
    font-weight: 950;
    text-transform: uppercase;
    transition: background var(--pm-transition-fast), border-color var(--pm-transition-fast);
  }

  a:hover {
    background: rgba(255, 255, 255, 0.28);
    border-color: var(--pm-white);
  }

  a:focus-visible {
    outline: none;
    box-shadow: var(--pm-focus-ring-yellow);
  }
</style>
