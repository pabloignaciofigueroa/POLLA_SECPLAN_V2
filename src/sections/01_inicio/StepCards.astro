---
import { INLINE } from "../../lib/ui-assets/uiAssets";

const steps = [
  {
    number: "01",
    title: "Aprende",
    description: "Explora reglas y datos clave.",
    tone: "blue",
    icon: INLINE.checklist,
  },
  {
    number: "02",
    title: "Elige",
    description: "Tu jugador y tu equipo favorito.",
    tone: "green",
    icon: INLINE.player,
  },
  {
    number: "03",
    title: "Predice",
    description: "Completa tus pronosticos.",
    tone: "purple",
    icon: INLINE.calendar,
  },
  {
    number: "04",
    title: "Gana",
    description: "Acumula puntos y sube.",
    tone: "orange",
    icon: INLINE.trophy,
  },
];
---

<ol class="steps" aria-label="Como jugar" data-stagger>
  {steps.map((step, index) => (
    <li class="step" data-tone={step.tone} data-animate="deal-in" style={`--i:${index + 1}`}>
      <span class="step-number">{step.number}</span>
      <span class="step-icon" aria-hidden="true">
        <img src={step.icon.src} alt="" width={step.icon.width} height={step.icon.height} loading="lazy" decoding="async" />
      </span>
      <span class="step-body">
        <span class="step-title">{step.title}</span>
        <span class="step-description">{step.description}</span>
      </span>
    </li>
  ))}
</ol>

<style>
  .steps {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: clamp(0.55rem, 1.15vw, 0.9rem);
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .step {
    position: relative;
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    grid-template-areas:
      "icon body";
    gap: 0.72rem;
    align-items: center;
    min-height: clamp(5.2rem, 7.2vw, 6.3rem);
    padding: 0.95rem 0.95rem 0.9rem;
    border: 1px solid color-mix(in srgb, var(--step-color, var(--pm-blue-500)) 28%, var(--pm-border-soft));
    border-radius: var(--pm-radius-md);
    background:
      radial-gradient(125% 85% at 6% -8%, color-mix(in srgb, var(--step-color, var(--pm-blue-500)) 20%, transparent), transparent 56%),
      linear-gradient(158deg, color-mix(in srgb, var(--step-color, var(--pm-blue-500)) 13%, var(--pm-white)) 0%, var(--pm-white) 62%);
    box-shadow:
      0 18px 36px color-mix(in srgb, var(--step-color, var(--pm-blue-500)) 24%, transparent),
      0 6px 14px rgba(7, 23, 53, 0.08),
      inset 0 1px 0 rgba(255, 255, 255, 0.85);
    overflow: hidden;
    transition: transform var(--pm-transition-base), box-shadow var(--pm-transition-base);
  }

  .step:hover {
    transform: translateY(-4px);
    box-shadow:
      0 26px 50px color-mix(in srgb, var(--step-color, var(--pm-blue-500)) 34%, transparent),
      0 8px 18px rgba(7, 23, 53, 0.10),
      inset 0 1px 0 rgba(255, 255, 255, 0.9);
  }

  .step::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 5px;
    background: linear-gradient(
      90deg,
      var(--step-color, var(--pm-blue-500)),
      color-mix(in srgb, var(--step-color, var(--pm-blue-500)) 35%, var(--pm-white))
    );
    box-shadow: 0 0 14px color-mix(in srgb, var(--step-color, var(--pm-blue-500)) 60%, transparent);
    pointer-events: none;
  }

  .step[data-tone="blue"] { --step-color: var(--pm-blue-500); }
  .step[data-tone="green"] { --step-color: var(--pm-mint-500); }
  .step[data-tone="purple"] { --step-color: var(--pm-purple-500); }
  .step[data-tone="orange"] { --step-color: var(--pm-orange-500); }

  .step-number {
    position: absolute;
    top: 0;
    left: 0.85rem;
    display: grid;
    min-width: 2rem;
    min-height: 1.65rem;
    place-items: center;
    border-radius: 0 0 var(--pm-radius-sm) var(--pm-radius-sm);
    background: var(--step-color, var(--pm-blue-500));
    color: var(--pm-white);
    font-family: var(--font-display);
    font-size: 0.72rem;
    font-weight: 950;
  }

  .step-icon {
    grid-area: icon;
    display: grid;
    place-items: center;
    width: clamp(2.7rem, 3.9vw, 3.45rem);
    aspect-ratio: 1;
    margin-top: 0.4rem;
    padding: 0.4rem;
    border-radius: 14px;
    background: linear-gradient(
      160deg,
      color-mix(in srgb, var(--step-color, var(--pm-blue-500)) 24%, var(--pm-white)),
      var(--pm-white) 78%
    );
    box-shadow:
      inset 0 0 0 1px color-mix(in srgb, var(--step-color, var(--pm-blue-500)) 28%, transparent),
      0 8px 16px color-mix(in srgb, var(--step-color, var(--pm-blue-500)) 24%, transparent);
  }

  .step-icon img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
    filter: drop-shadow(0 4px 7px color-mix(in srgb, var(--step-color, var(--pm-blue-500)) 30%, transparent));
  }

  .step-body {
    grid-area: body;
    display: flex;
    flex-direction: column;
    gap: 0.18rem;
    min-width: 0;
    margin-top: 0.25rem;
  }

  .step-title {
    color: var(--step-color, var(--pm-blue-500));
    font-family: var(--font-display);
    font-size: clamp(0.96rem, 1.35vw, 1.25rem);
    font-weight: 950;
    letter-spacing: 0.04em;
    line-height: 1;
    text-transform: uppercase;
  }

  .step-description {
    color: var(--pm-blue-900);
    font-size: clamp(0.68rem, 0.8vw, 0.78rem);
    font-weight: 650;
    line-height: 1.22;
  }

  @media (max-width: 1080px) {
    .steps {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 520px) {
    .steps {
      grid-template-columns: 1fr;
    }
  }
</style>
